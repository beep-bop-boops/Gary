Gary Works

- Userinetface is made with Tkinter, a python lib
- The program manipulates a powershell command based on user inputs
- once the command is sent it creates a excel file with the data in temp folder which to pgoram uses to show the data
- if user exports copys that temp folder to users desierd location

